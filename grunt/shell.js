module.exports = {
    // configure shell commands ------------------------------------------------
    updateCanIUse: {
        command: 'npm update caniuse-db'
    },
    bower: {
        command: 'bower update && bower install'
    }
};
